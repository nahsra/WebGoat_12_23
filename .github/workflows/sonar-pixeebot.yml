name: MULTIPLE FILES
on:
  check_run:
    types: [completed]

permissions:
  id-token: write

jobs:
  share:
    name: Testing URL
    runs-on: ubuntu-latest
    if: ${{ github.event.check_run.name == 'SonarCloud Code Analysis' }}
    steps:
      - uses: pixee/upload-tool-results-action@v1.1.1
        with:
          pixee-api-url: https://www.carlos.com
          tool: sonar
          sonar-token: ${{ secrets.SONAR_TOKEN }}
